#!/bin/sh /etc/rc.common
# Copyright (C) 2015 OpenWrt.org

START=99

start() {
	modprobe sun4i_can
	/usr/bin/ip link set can0 up type can bitrate 250000
	sleep 10

	[ -e /www/config/ ] && {
		can2lan -m -c /www/config -b br-lan
	}
}

stop() {
	killall can2lan
}
