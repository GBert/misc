--- a/makefile
+++ b/makefile
@@ -72,6 +72,7 @@ uninstall:
 
 version:
 	echo $(QUOT)const int bzr = $(QUOT) > common$(FS)version.h
-	bzr revno >> common$(FS)version.h
+	echo $(QUOT)5865$(QUOT) >> common$(FS)version.h
+#	bzr revno >> common$(FS)version.h
 	echo $(QUOT);$(QUOT) >> common$(FS)version.h
 
