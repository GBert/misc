#include <stdio.h>
#include <string.h>

#define BITS_PER_TXBYTE	5

void print_sequence(char *sequence) {
  int i;
  printf("Value: %s\n", sequence);
}
 
int main() {
  uint8_t data[256];

  
  return 0;
}
